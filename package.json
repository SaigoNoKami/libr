{
  "name": "freelans",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint:client": "npm --prefix client run lint:fix && npm --prefix client run format:fix",
    "lint:server": "npm --prefix server run lint:fix && npm --prefix server run format:fix",
    "prepare": "husky install"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "husky": "^8.0.0"
  },
  "dependencies": {}
}
